/* 디버그 메뉴 ON/OFF 스위치
   - 이 값은 "디버그탭(챕터 바로가기 메뉴)이 보이느냐"만 결정합니다.
   - 진행 순서 잠금(flow-guard.js)은 이 값과 무관하게 항상 작동합니다.
   매장 운영 시작하면 false로 두고, 개발/테스트할 때만 true로 바꿔서
   이 파일 하나만 레포에 올리면 22개 페이지 전체에 반영됩니다. */
var RCM_DEBUG_MODE = false;
