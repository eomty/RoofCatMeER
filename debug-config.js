/* 디버그탭(챕터 바로가기 메뉴) 표시 여부.
   - 진행 순서 잠금(flow-guard.js)은 이 값과 완전히 무관하게 항상 작동합니다.
     (주소창 직접 입력은 이 값에 상관없이 항상 막힘)
   - 이 값은 오직 "디버그탭이 화면에 보이느냐"만 결정합니다.
   매장 운영 시작해서 손님에게 디버그탭을 숨기고 싶을 때만 false로 바꾸세요. */
var RCM_DEBUG_MODE = true;
